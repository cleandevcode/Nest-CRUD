export enum LengthUnit {
  cm = 'cm',
  in = 'in',
  ft = 'ft',
}

export enum WeightUnit {
  kg = 'kg',
  lb = 'lb',
}

export enum ClientSortKey {
  id = 'id',
  name = 'name',
  lastClaim = 'lastClaim',
}

export enum PlanType {
  Primary = 'primary',
  Secondary = 'secondary',
}